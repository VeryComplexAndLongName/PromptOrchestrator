# PromptOrchestrator

Python module for structured prompt orchestration with static/semi-stable/dynamic layout,
configurable summary provider selection, TTL cache backends, optional RAG providers,
safety checks, prompt efficiency analysis, token counting with `tiktoken`, centralized
mutable Pydantic config, and one-call bootstrap from a config store.
